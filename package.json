{
  "name": "football-predictor",
  "version": "1.0.0",
  "description": "End-to-End Football Match Predictor with React Frontend and FastAPI Backend",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  }
}
