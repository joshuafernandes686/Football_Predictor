# FootballPredictor Environment Variables (Optional)

# LLM API Keys (Leave blank to use the built-in Grounded Local Provider)
OPENAI_API_KEY=
GEMINI_API_KEY=

# LLM Provider Selection ("auto", "openai", "gemini", or "local")
LLM_PROVIDER=auto
